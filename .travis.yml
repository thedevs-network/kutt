language: node_js
node_js:
  - "9"

before_install: 
   - sudo bash ./docker-examples/before-install.sh

script:
  - npm run lint:nofix
  - npm test
  - npm build
  - docker-compose up --build -d
  # - yarn test:e2e

after_script:
  - docker-compose down
