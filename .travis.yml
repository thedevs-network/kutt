language: node_js
node_js:
  - "10"

before_install: chmod a+x ./docker-examples/before-install.sh

script:
  - npm run lint:nofix
  - npm test
  - npm build
  - docker-compose up --build -d
  # - yarn test:e2e
