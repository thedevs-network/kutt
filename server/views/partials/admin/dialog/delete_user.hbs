<div class="content">
  <h2>{{t "table.admin.deleteUserDialogTitle"}}</h2>
  <p>
    {{{t "table.admin.deleteUserDialogMsg" email=email}}}
  </p>
  <div class="buttons">
    <button type="button" hx-on:click="closeDialog()">{{t "backbone.cancel"}}</button>
    <button 
      type="button"
      class="danger confirm" 
      hx-delete="/api/users/admin/{id}" 
      hx-ext="path-params" 
      hx-vals='{"id":"{{id}}"}' 
      hx-target="closest .content" 
      hx-swap="none" 
      hx-indicator="closest .content"
      hx-select-oob="#dialog-error"
    >
      <span>{{> icons/trash}}</span>
      {{t "backbone.delete"}}
    </button>
    {{> icons/spinner}}
  </div>
  <div id="dialog-error">
    {{#if error}}
      <p class="error">{{error}}</p>
    {{/if}}
  </div>
</div>